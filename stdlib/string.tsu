## An empty string literal
empty := ""

## Valid line terminators. /n is the standard for UNIX systems, while
## /r/n is used by default on Windows machines. 
line_terminators := ["/n", "/r/n"]

# Valid whitespace characters
space_characters := [" ", "/t", "/n", "/r/n"]

