## Type safety checks

var aString  : string = "A string (type annotated)"
aString_ := "A string (type inferred)"

var aInt : int = 123
# same as above, you can infer its type
JS_print("The value of 'aInt' is: ")
JS_print(JS_toString(aInt))

# -*- vim: set ft=ruby shiftwidth=4 softtabstop=4 tabstop=4 expandtab -*-
